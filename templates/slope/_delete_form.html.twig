<form method="post" action="{{ path('app_slope_delete', {'id': slope.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ slope.id) }}">
    <button class="btn">Delete</button>
</form>
